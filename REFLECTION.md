# Mecha — REFLECTION.md

> 1500-2500 字反思报告。回答 AI4SE 课程提出的核心问题：当 LLM 能完成大部分编码工作时，一个工程师的真正价值在哪里？

---

## Superpowers 方法论：哪些发挥了作用，哪些形式大于实质

坦诚地说，本项目没有严格使用 Superpowers 框架。我使用的是 Claude 桌面端，而非安装了 Superpowers 插件的编码智能体。这个"偏离"本身就是一个值得反思的起点。

Superpowers 的核心价值在于它提供的流程纪律。它的 brainstorming 技能要求逐步签字确认设计，writing-plans 技能强制将模糊想法拆为可执行 task，test-driven-development 技能则强制执行红-绿-重构循环。这些纪律在 AI 协作中极其容易松懈——当你看到 agent 在几秒内吐出几百行代码时，很难克制住"直接跑一下看看"的冲动。Superpowers 用代码机制替你守住了这些底线。

但我在本项目中，通过人工自律达到了类似的效果。Brainstorming 阶段，我主动要求 Claude 在每项决策后总结确认；SPEC 完成后，我严格执行了冷启动验证；测试阶段，我确保每个模块都有对应的 Mock LLM 测试。这说明 Superpowers 解决的核心问题不是技术问题，而是纪律问题——它给开发者一套"流程脚手架"，防止他们在 AI 的快速反馈中跳过关键步骤。换句话说，Superpowers 的价值不在于它做了什么，而在于它阻止你跳过什么。

如果硬要说哪些"形式大于实质"，我认为是固定的七步工作流。不同项目的复杂度、领域、团队规模差异巨大，强行要求每一步都按顺序执行，可能在简单项目上浪费时间，在复杂项目上又不够深入。更好的设计或许是一套可配置的流程模板，根据项目类型推荐不同的流程深度，而不是对所有项目一视同仁。

---

## TDD 在 AI 协作下：阻碍还是放大器

本项目的实践给出了一个矛盾的答案。

从正面看，TDD 在 AI 协作下是放大器。Mock LLM 测试让我能在不调用任何真实 API 的情况下验证护栏、反馈、主循环的正确性。67 个测试在 0.6 秒内跑完，每次修改代码后立刻得到反馈——这种快速循环是传统 TDD 难以达到的。尤其是护栏模块，18 个测试覆盖了规则匹配、分级判定、审计日志的每个分支，每一次 `assert` 都在告诉我：这个函数的行为是确定的，不依赖 LLM 的随机性。这种确定性带来的信心，是任何"看起来对了"的代码都无法提供的。

但从反面看，本项目没有严格遵循红-绿-重构循环。测试和实现是同时写的，而非先写失败测试再写最少代码使其通过。原因很简单：当你用 AI 批量生成代码时，"先写一个测试，看它变红，写最少代码，看它变绿，重构"的节奏被打乱了。AI 一次性输出完整的模块实现和对应的测试，你很难在中间插入"红"的步骤——要么全部接受，要么全部重来。

我认为问题不在于 TDD 本身，而在于 TDD 的颗粒度需要适配 AI 协作模式。与其在一个 task 内严格红-绿-重构，不如在 task 间做 TDD：每个 task 的验收标准就是它的"测试"，task 完成前验收标准不通过就是"红"，task 完成后验收标准通过就是"绿"。PLAN 中每个 task 的"验证步骤"列，本质上就是这种"task 级 TDD"的体现。红-绿-重构的节奏没有消失，只是从函数级上移到了模块级。

---

## Subagent 工作流：能自主运行多久

冷启动验证提供了最直接的证据。我用一个完全不同的编码智能体，仅凭 SPEC.md 和 PLAN.md，让它实现 T1（项目骨架）和 T5（Tools 模块）。结果是 14 个测试全部通过，agent 全程未主动提问。

这说明 SPEC 和 PLAN 对基础模块的颗粒度是足够的。一个陌生的 agent 在没有任何对话历史的情况下，能独立完成两个模块的实现，说明文档质量确实过关。但与此同时，agent 没有提问不等于没有做假设。它在 T2（数据模型）的处理上做了自己的判断——将 T2 合并入 T1 实现——这个判断本身是合理的，但 PLAN 没有明确说明是否允许这种合并。如果 agent 在更复杂的模块上做了类似的"合理但未声明的假设"，比如在护栏的三层结构中自作主张地跳过某一层，结果可能就不一样了。

关于"自主运行多久不偏离"，我的观察是：task 的颗粒度决定了偏离的上限。一个 2-5 分钟的 task，agent 偏离的概率很低，因为 task 本身足够简单，几乎没有"发挥空间"。但如果 task 颗粒度太大——比如"实现护栏"——agent 可能在实现过程中忘记某个边界条件，或者自行"简化"设计。PLAN 的质量直接决定了 agent 的行为质量，这是本项目最深刻的教训之一。最好的 PLAN 不是"写得很详细"的 PLAN，而是"让 agent 没有机会走偏"的 PLAN。

---

## SPEC/PLAN 质量如何影响实现质量

在实现 T6（护栏）时，我遇到了一个具体问题：正则表达式 `rm\s+(-[rRf]+\s+)*/` 能匹配 `rm -rf /`，但无法匹配 `rm --recursive --force /`。这是 SPEC 中"已知限制"一节预期到的问题，但预期归预期，真正在代码中看到这个漏洞时，感受完全不同。纸上写"可能存在边界情况"是一回事，亲眼看到自己的护栏被一个简单的参数形式绕过是另一回事。

如果 SPEC 中写得更具体——比如给出一个"护栏应拦截但当前无法拦截"的示例清单——PLAN 中就可以为这个清单中的每项设计对应的任务，例如"实现命令展开预处理，将 `--recursive` 展开为 `-r` 等效形式"。但 SPEC 只写了"无法覆盖所有恶意命令变体"，这个表述太模糊，无法转化为可执行的 task。模糊的 SPEC 产生模糊的 PLAN，模糊的 PLAN 产生不可预期的实现。

这个案例让我认识到，SPEC 中的"已知限制"不只是免责声明，而是未来工作的种子。每项限制都应该附带一个具体示例和一个可行的改进方向，这样 PLAN 才能真正利用 SPEC 的信息。如果 SPEC 只是泛泛地承认"有局限"，而不指出局限的具体形态，那它就对后续实现没有指导价值——它只是在说"我知道有问题，但我没打算在这轮解决"，而一个好的 spec 应该说的是"我知道有问题，这是问题的具体形状，如果你有多余的时间，可以往这个方向修"。

---

## 最有效的 Prompt/Context 策略

在 SYSTEM_PROMPT 中，最关键的一句话是"Respond ONLY with a JSON object in this exact format"。没有这句话，DeepSeek 倾向于在 JSON 前后添加解释文字，比如"好的，我来执行这个操作，以下是 JSON..."，导致 `_parse_action` 解析失败。加上这句话后，LLM 的输出格式稳定性大幅提升。这个发现看似微不足道，但它揭示了一个更一般的规律：LLM 的输出格式约束比内容约束更重要。内容质量——"写得好不好"——可以通过反馈闭环修正，但格式错误——"不是合法 JSON"——会直接中断主循环，而 LLM 自己很难从格式错误中恢复，因为它根本不知道自己的输出被怎么解析的。在 harness 的设计中，花在"约束 LLM 输出格式"上的精力，应该不亚于花在"设计反馈闭环"上的精力。

另一个有效策略是在 feedback 中提供结构化的错误信息。当测试失败时，Feedback 模块不只是把 pytest 的原始输出丢给 LLM，而是解析出 `FAILED test_foo.py::test_add - AssertionError: assert 3 == 5` 这样的结构化信息。LLM 对结构化反馈的响应质量远高于原始输出——因为结构化信息减少了 LLM 的"理解负担"，让它能把精力集中在"如何修复"上，而不是"理解发生了什么"上。这本质上是一种"上下文工程"：不是给 LLM 更多的信息，而是给 LLM 更容易消化的信息。

---

## 凭据与分发：迫使我想清楚了什么

这两个工程要求是项目中最能体现"工程师思维"的部分。

凭据方面，最早的设计是把 API Key 存在 `.env` 文件里，这在原型阶段很常见，几乎是一种肌肉记忆。但项目要求迫使我停下来思考：`.env` 是明文文件，可能被误提交 Git、被备份工具上传、被日志记录。于是我设计了 keyring 主方案加加密文件降级方案。在实现过程中，又发现 Docker 容器内 keyring 可能不可用，再补充了 fallback 的检测逻辑。这个层层迭代的过程让我意识到，凭据安全不是"选一个方案"的问题，而是"在多个不完美的方案之间做有意识的权衡"。没有一个方案是完美的——keyring 依赖操作系统后端，加密文件需要主密码，环境变量是明文——工程师的职责不是找到完美的方案，而是理解每种方案的威胁模型，选择最合适的组合，并清楚地告知用户这些方案的风险。

分发方面，Docker 看起来很简单，但实际写 Dockerfile 时暴露了大量隐性假设：Python 版本、依赖安装顺序、volume 挂载策略、ENTRYPOINT 设计。如果我没有被要求写 Dockerfile，这些假设会一直停留在"我以为"的层面，直到第一个用户报告"跑不起来"。这两个要求教会我的是：一个项目的"完成"不是代码写完，而是别人能安全地跑起来。代码写完只是项目的一半，另一半是让一个完全不了解你项目的人，在一条命令之内，安全地把它跑起来。

---

## 如果重做，我会改变什么

如果能重来，有几个地方我会做出不同的选择。

冷启动验证我会选更复杂的 task。我只验证了 T1 和 T5，这是项目中最简单的模块。如果重来，我会选 T6（护栏）或 T9（主循环）——这些才是项目的核心，也是最容易暴露 SPEC 缺陷的地方。用简单模块做验证虽然能快速通过，给人"SPEC 没问题"的错觉，但可能错过真正需要检验的部分。好的验证不是验证最简单的东西，而是验证最可能出错的东西。

我会严格使用 Superpowers 框架。本项目没有使用 Superpowers，虽然通过人工自律达到了类似效果，但缺少了它的自动化流程保障。如果重来，我会安装 Superpowers 插件，体验它从 brainstorming 到 finishing 的完整流程，这样才能对"Superpowers 到底有没有用"给出有依据的判断。现在反思中关于 Superpowers 的批判，有一部分是基于文档阅读而非实际体验，这不够扎实——就像一个从来没有用过 vim 的人在批评 vim 的学习曲线。

护栏方面，我会做更深的命令解析。当前护栏基于正则匹配，这有明显的局限性。正则匹配是"尽力而为"的——它看到 `rm -rf` 就拦截，但看到 `rm --recursive --force` 就可能放过去。如果重来，我会实现一个简单的命令解析器，至少能识别 `rm` 的各种参数形式，让护栏的覆盖率更高。正则匹配和命令解析之间的差距，不是"多做一点"的问题，而是"质的区别"：前者是模式匹配，后者是语义理解。

此外，我会提前设计 AGENT_LOG 的记录格式。AGENT_LOG 是在项目结束后补写的，虽然内容准确，但缺少实现过程中的即时细节——那些在调试失败时冒出来的想法、临时改动的理由、绕过的坑，事后很难靠回忆补齐。如果重来，我会在 PLAN 中为每个 task 预留一个"日志槽"，完成一个填一个，而不是事后追忆。

---

## 对 Superpowers 方法论的批判

Superpowers 的核心假设是：AI 编码的问题在于流程纪律，而非技术能力。这个假设在本项目中部分成立。

成立的部分是，TDD、code review、planning 这些纪律确实在 AI 协作中容易松懈。当一个 subagent 在 30 秒内完成了你以为需要 30 分钟的任务，你很容易跳过"验证它是否真的正确"的步骤——不是因为你懒，而是因为人类的大脑在效率面前会自动降低警惕性。Superpowers 的流程强制力解决了这个问题。在本项目中，即使没有使用 Superpowers，我也感受到了这种"流程强制力"的必要性——如果不是项目要求明确写了"必须先写 SPEC 再写代码"，我可能真的会跳过设计阶段直接开始写代码。规则的存在不是为了约束你，而是为了在你最容易松懈的时刻拉住你。

不成立的部分是，Superpowers 假设开发者使用的是支持其技能系统的特定编码智能体，如 Claude Code 的 hooks 和 skills。但现实中，开发者可能使用任何工具——Cursor、GitHub Copilot、ChatGPT、甚至像我一样使用 Claude 桌面端。Superpowers 的"技能"在这些非标准环境中不可用，它的价值就被削弱为"一套流程文档"。这暴露了 Superpowers 的一个架构假设：工具耦合度高，可移植性差。一个好的方法论应该独立于具体的工具，但 Superpowers 的当前实现是深度绑定在特定编码智能体的 plugin 机制上的。如果它的价值有一半在流程设计、一半在工具集成，那离开了工具，它就只剩一半。

更深层的批判是，Superpowers 的方法论假设流程的标准化能覆盖项目的多样性。但不同类型的项目对流程的需求完全不同。一个 harness 项目需要更深入的 mock 测试设计，而一个应用类项目可能需要更深入的前端设计流程。Superpowers 的七步工作流是"一刀切"的，它没有提供根据项目类型调整流程深度的机制。或许未来版本的 Superpowers 可以提供"流程模板"——比如"harness 项目模板"默认启用更激进的 TDD 和 code review，而"应用类项目模板"则在前端设计阶段投入更多步骤。一刀切的流程不是坏流程，但它一定不是最优流程。

---

## 结语：工程师的真正价值

回到课程的核心命题：当 LLM 能完成大部分编码工作时，一个工程师的真正价值在哪里？

本项目的答案是：价值在 harness 层。不是"写代码"本身，而是定义什么是正确的反馈信号，设计系统的安全边界，建立独立于 LLM 的验证机制，以及让系统能被他人安全地使用。

什么样的反馈信号是客观的、可回灌的？什么样的行为是危险的、需要拦截的？这些定义不能交给 LLM——LLM 只会给你一个"听起来合理"的答案，但工程需要的是"确定正确"的答案。在本项目中，护栏的规则列表、反馈的解析格式、Mock LLM 的响应序列，都是我来定义的，LLM 在这些问题上帮不上忙，因为它不知道这个系统在什么情况下会失败。护栏的规则列表、配置的默认值、反馈的解析格式——这些边界条件需要理解系统在什么情况下会失败，而 LLM 无法自己设计这些，它只能在你定义了边界之后，在边界内运行。67 个测试在 0.6 秒内跑完，三个演示脚本全部通过——这些客观证据比 LLM 说的"我完成了"可靠得多。工程的价值在于不信任 LLM 的自我评估，而是建立独立的验证机制。凭据安全、Docker 分发、README 文档——这些不是传统意义上的"编码"工作，但它们是让代码成为产品的关键。LLM 可以帮你写代码，但它不会主动提醒你这个 Key 不能提交到 Git，也不会替你设计 Docker 的 volume 挂载策略。

简而言之，LLM 是大脑，harness 是神经系统。大脑负责思考，神经系统负责让思考的结果安全、可靠、可验证地转化为行动。工程师的价值不在思考层，而在神经系统层。

---

*本文由 AI 辅助润色。具体润色内容如下：*

**Superpowers 章节**：补充了"当你看到 agent 在几秒内吐出几百行代码时，很难克制住'直接跑一下看看'的冲动"；结尾增加了"Superpowers 的价值不在于它做了什么，而在于它阻止你跳过什么"。

**TDD 章节**：补充了"每一次 `assert` 都在告诉我：这个函数的行为是确定的，不依赖 LLM 的随机性。这种确定性带来的信心，是任何'看起来对了'的代码都无法提供的"；结尾增加了"红-绿-重构的节奏没有消失，只是从函数级上移到了模块级"。

**Subagent 章节**：结尾增加了"最好的 PLAN 不是'写得很详细'的 PLAN，而是'让 agent 没有机会走偏'的 PLAN"。

**SPEC/PLAN 章节**：结尾增加了"它只是在说'我知道有问题，但我没打算在这轮解决'，而一个好的 spec 应该说的是'我知道有问题，这是问题的具体形状，如果你有多余的时间，可以往这个方向修'"。

**Prompt 策略章节**：补充了"这个发现看似微不足道，但它揭示了一个更一般的规律"；结尾增加了"这本质上是一种'上下文工程'：不是给 LLM 更多的信息，而是给 LLM 更容易消化的信息"。

**凭据与分发章节**：补充了"几乎是一种肌肉记忆"；补充了"工程师的职责不是找到完美的方案，而是理解每种方案的威胁模型，选择最合适的组合，并清楚地告知用户这些方案的风险"；结尾增加了"代码写完只是项目的一半，另一半是让一个完全不了解你项目的人，在一条命令之内，安全地把它跑起来"。

**重做章节**：补充了"给人'SPEC 没问题'的错觉"；增加了"好的验证不是验证最简单的东西，而是验证最可能出错的东西"；增加了"就像一个从来没有用过 vim 的人在批评 vim 的学习曲线"；增加了"正则匹配和命令解析之间的差距，不是'多做一点'的问题，而是'质的区别'：前者是模式匹配，后者是语义理解"；补充了"那些在调试失败时冒出来的想法、临时改动的理由、绕过的坑"。

**批判章节**：补充了"不是因为你懒，而是因为人类的大脑在效率面前会自动降低警惕性"；增加了"规则的存在不是为了约束你，而是为了在你最容易松懈的时刻拉住你"；增加了"如果它的价值有一半在流程设计、一半在工具集成，那离开了工具，它就只剩一半"；结尾增加了"一刀切的流程不是坏流程，但它一定不是最优流程"。

**其余部分为初稿，未做修改。**