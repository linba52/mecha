# Mecha — SPEC_PROCESS.md

> 记录与 Claude 协作生成 SPEC 与 PLAN 的全过程，包括 brainstorming 关键节点、冷启动验证发现、以及 SPEC 修订记录。

---

## 一、Brainstorming 关键节点

### 1.1 项目方向确定

**初始状态**：已知选择 A 类项目（Coding Agent Harness），但无具体方向。

**第一轮追问**：智能体首先询问了三个方向性问题——项目类型（A/B）、当前阶段（从零开始/有方向/有清晰想法）、技术偏好。确认了 A 类、从零开始、语言未定。

**关键决策点 1：重点维度选择**

智能体分析了四个可选维度（治理/护栏、反馈闭环、工具/扩展、记忆/上下文），推荐了治理/护栏作为重点维度。理由：护栏天然是代码而非提示词，最容易满足"移除 LLM 后还能单测验证"的硬要求，三层递进结构（规则匹配→分级审批→审计日志）天然体现工程深度。

**我的决策**：采纳。这个分析说服了我——护栏的每一个判定都是确定性逻辑，不需要 LLM 的"智能"介入，这是其他维度难以做到的。

**关键决策点 2：技术栈选择**

智能体推荐了 Python + Docker 的组合。理由：Python 的 LLM SDK 生态最成熟（openai SDK 直接兼容 DeepSeek），Docker 分发最通用。对比了 Go/Rust 的优缺点——单文件二进制分发更优雅，但 LLM 调用、JSON 解析等日常操作会多写很多样板代码。

**我的决策**：采纳。时间性价比是首要考虑，Python 能让我把精力集中在 harness 核心逻辑上。

**关键决策点 3：LLM 供应商**

最初选型为 Anthropic Claude，但后来改为 DeepSeek。理由：DeepSeek 兼容 OpenAI SDK 接口，`openai` Python 库改 `base_url` 即可直接调用，无需额外适配工作。

**我的决策**：采纳。这个改动影响很小，但降低了 API 成本。

**关键决策点 4：项目形态**

智能体提出了两种形态——A（单次任务执行型）和 B（交互会话型），推荐了 A。理由：主循环更清晰、更容易测试、机制演示更自然、时间花在刀刃上。

**我的决策**：采纳。形态 A 的终止条件明确（任务完成或达到最大迭代），Mock 测试更可控。

**关键决策点 5：项目命名**

智能体提供了几个备选名（CodeGuard、Sentinel、SafeCoder），我提出想要"机甲"的英文。智能体给出 Mecha，寓意"人驾驶的机器"——对应 harness 的核心概念：人通过护栏和审批驾驭 LLM。我采纳了。

### 1.2 AI 提出而我采纳的关键建议

1. **三层护栏结构**（规则匹配 → 分级审批 → 审计日志）：智能体主动提出的递进设计，三层之间有清晰的逻辑关系，每层都可以独立测试。
2. **Docker 分发方案**：智能体解释了 Docker 相比二进制/包管理器的优势——最通用、最不容易出问题、CI 集成最成熟。
3. **Mock LLM 测试策略**：智能体强调"移除 LLM 后还能用单测验证"是区分"编码了机制"与"只写了提示词"的硬标准，这个判据贯穿了整个设计。
4. **冷启动验证选 T1+T5**：智能体建议用 T1（项目骨架）测试基础结构清晰度，用 T5（Tools）测试模块规约完整性，两者组合能在 10 分钟内暴露 SPEC 缺陷。

### 1.3 我推翻或修正的 AI 建议

1. **LLM 供应商从 Anthropic 改为 DeepSeek**：智能体最初推荐 Anthropic（因为课程使用 Claude），但我选择了 DeepSeek。理由：兼容 OpenAI SDK、成本更低。
2. **无其他重大推翻**：整体设计方向与智能体的建议高度一致，这反映了 brainstorming 阶段我问得比较清楚，智能体的追问也帮助我澄清了模糊之处。

---

## 二、关键迭代对话节选

### 迭代 1：从模糊到具体

**我问**：什么是 harness？

**智能体回答**：用大白话解释了 harness = LLM + 工程外壳，六件事（决策/工具/护栏/反馈/记忆/配置），并建议我造一个迷你版的 Claude Code。

**我的反应**：这个比喻（"自己造一个迷你版的 Claude Code"）让我立刻理解了项目的本质。我确认了方向。

### 迭代 2：模块拆分

**智能体**：画了架构图，拆了 6 个模块，每个用一句话定义。

**我的反应**：架构图清晰，但我最关心的是护栏怎么做深。智能体提出三层结构后，我追问每层的具体实现方式，确认了规则匹配用正则、分级审批用状态机、审计日志用 JSONL。

### 迭代 3：数据模型与凭据

**智能体**：定义了 4 个 dataclass（Action, ActionResult, GuardrailDecision, MemoryEntry）和凭据生命周期（录入→使用→查看→更新→清除）。

**我问**：什么是数据模型？什么是凭据安全方式？

**智能体**：用门钥匙比喻解释 API Key 安全，用"约定数据长什么样"解释数据模型。

**我的反应**：这些比喻帮助我理解了概念，但我更关注实现细节。确认了 keyring 作为主方案、加密文件作为降级方案。

### 迭代 4：部署方式

**我补充要求**：课程方提供了两种部署方案——仅 CLI（GitHub Release 链接）或 CLI + WebUI。

**智能体**：记录了这两种方案，并建议选方案一（仅 CLI），把精力集中在 harness 内核上。

**我的决策**：采纳方案一。

---

## 三、冷启动验证

### 3.1 验证设置

- **验证 agent**：不同于 Claude 的另一个编码智能体
- **输入**：仅提供 SPEC.md + PLAN.md，不提供任何对话历史或口头解释
- **任务**：实现 T1（项目骨架搭建）和 T5（Tools 模块）
- **指令**：遇到不确定之处即暂停询问，不凭猜测继续

### 3.2 验证结果

**T1 完成情况**：顺利创建了完整的项目结构，包括 pyproject.toml、mecha 包、CLI 入口、数据模型、LLM 抽象层。产物结构与 SPEC 描述一致。

**T2 完成情况**：Agent 将 T2 与 T1 合并实现，一起完成了数据模型和 BaseLLM 抽象类。这个合并是合理的（T2 依赖 T1），但 PLAN 未明确标注这种合并是被允许的。

**T5 完成情况**：实现了 read_file、write_file、run_command 三个工具，所有测试通过（14/14）。路径穿越拦截、文件大小限制、命令执行超时等边界条件都正确处理。

### 3.3 暴露的 SPEC 缺陷

**缺陷 1：PLAN 未说明 T2 可与 T1 合并**

冷启动 agent 将 T2 与 T1 合并实现，这虽然合理，但 PLAN 中 T2 被列为独立任务，agent 的判断是基于常识而非 SPEC 指引。

**修订**：在 PLAN 中补充说明 T1 和 T2 由于依赖关系紧密，可以在一次 subagent 会话中合并实现。

**缺陷 2：SPEC 未明确 `.mecha.yaml` 的默认搜索路径**

Agent 在实现 Config 时假设配置文件在项目根目录，但 SPEC 中只写了"项目根目录 `.mecha.yaml`"，未说明搜索优先级和 fallback 行为。

**修订**：已确认 Config 模块的默认行为（从当前目录读取，缺失时使用默认值），SPEC 中已包含此描述，无需修订。

**缺陷 3：无重大阻塞性问题**

冷启动 agent 在整个过程中没有主动提问或暂停——这意味着 SPEC 和 PLAN 对 T1/T2/T5 的颗粒度是足够的。但这可能也意味着 agent 在某些地方做了假设而未提问，需要在后续集成阶段保持警惕。

### 3.4 冷启动验证结论

SPEC 和 PLAN 对基础模块（T1/T2/T5）的描述足够清晰，冷启动 agent 能够独立完成实现。没有发现需要大幅修订 SPEC 的缺陷。这是一个正面信号，但不应过度解读——T1/T2/T5 是项目中最简单的模块，复杂的模块（T6 护栏、T9 主循环）可能暴露更多问题。

---

## 四、对 Brainstorming 过程的反思

### 做得好的地方

1. **追问质量**：智能体在关键决策点（重点维度、技术栈、项目形态）都给出了多选项对比分析，每个选项都有明确的 pros/cons，帮助我做出有依据的决策。
2. **大白话解释**：当我对概念不理解时（harness、数据模型、凭据安全、Docker），智能体用比喻和具体例子解释，让我快速建立认知。
3. **逐步签字确认**：每个设计决策完成后，智能体会总结并确认，然后再进入下一步，避免了"一次性堆砌大量设计文档然后发现理解有偏差"的问题。

### 做得不够好的地方

1. **缺少挑战性追问**：智能体在大部分决策上倾向于给出推荐并解释理由，但较少主动追问我的假设。例如，当我选择"从零开始"时，智能体没有追问"你是否有特定的编码痛点想解决？"——这类问题可能会让项目方向更聚焦。
2. **用户故事是我的弱项**：智能体提出了 5 个用户故事，我基本全部接受，没有深入质疑每个故事的 INVEST 合规性。这说明我在产品思维层面还需要更多训练。
3. **冷启动验证的局限性**：只验证了 T1+T5（最简单的基础模块），没有验证护栏（T6）或主循环（T9）这些复杂模块。但这受限于验证时间（1-2 小时），如果重来，我会选择 T6（护栏）作为验证对象，因为它才是项目的核心。

---

## 五、SPEC 修订记录

| 日期 | 修订内容 | 触发来源 |
|------|----------|----------|
| 2026-08-15 | 初始版本 | Brainstorming |
| 2026-08-15 | LLM 供应商从 Anthropic 改为 DeepSeek | 用户决策 |
| 2026-08-15 | 部署方案补充（方案一：仅 CLI + Release 链接） | 课程方补充要求 |
| 2026-08-15 | PLAN 补充说明 T1/T2 可合并实现 | 冷启动验证发现 |